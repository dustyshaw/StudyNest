import AddCourseForm from "./AddCourseForm";
import FormLayout from "../LayoutComponents/FormLayout";

const AddCourse = () => {
  return (
    <FormLayout formTitle="Add Course">
      <AddCourseForm />
    </FormLayout>
  );
};

export default AddCourse;
